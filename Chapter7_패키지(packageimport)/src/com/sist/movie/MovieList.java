package com.sist.movie; // 현재 폴더위치 지정
import com.sist.board.*; // 다른 패키지에 있는 클래스를 사용
// 라이브러리 => import
/*
 *   생략 : import java.lang.*; => 모든 클래스를 불러온다
 *               -------------  String, Math, System
 *   => 기본 클래스명을 로딩
 *      => 2개이상 사용
 *      java.util.List
 *      java.util.Map
 *      java.util.Scanner
 *      ------------------- java.util.*
 */
public class MovieList {
	BoardList b= new BoardList();
	void display()
	{
		
	}
}
